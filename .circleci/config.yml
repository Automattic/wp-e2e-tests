version: 2
jobs:
  build:
    working_directory: ~/wp-e2e-tests
    docker:
      - image: debian:jessie
      - image: selenium/standalone-chrome

    steps:
      - checkout
      - run: sleep 5 && nc -vz localhost 4444
