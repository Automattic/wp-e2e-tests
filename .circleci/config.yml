version: 2
jobs:
  build:
    working_directory: /wp-e2e-tests
    docker:
      - image: hoverduck/wp-e2e-tests:0.0.1
        environment:
                NODE_ENV: test
    steps:
      - checkout
      - run: npm install
      - run: xvfb-run ./node_modules/.bin/magellan --max_test_attempts=1 --max_workers=3
