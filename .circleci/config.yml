version: 2
jobs:
  build:
    working_directory: ~/wp-e2e-tests
    docker:
      - image: buildpack-deps:trusty
      - image: selenium/standalone-chrome

    steps:
      - checkout
      - run: sleep 5 && nc -vz localhost 4444
